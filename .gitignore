# Secrets / env
.env

# Python
__pycache__/
*.pyc

# macOS
.DS_Store

# Local data exports / trip logs (do NOT commit)
/exports/
*.export.csv
*.log

# NOTE: the dashboard PID CSVs under obd/ are tracked content — do NOT ignore them.
# (The *.export.csv pattern above is intentionally specific so it never matches
# obd/**/dashboards/**/*-pids.csv.)
