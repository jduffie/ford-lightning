---
paths: ["**/*-pids.csv", "**/dashboards/**/README.md"]
---

Never write a PID hex address you cannot cite to a forum PID sheet or a value
the user pasted. Unverified → `TODO`/`UNVERIFIED`, never a guess. Keep ≤10 PIDs
per dashboard page.
